# Frontend Environment Variables Example

# Backend API URL
# Use localhost when running locally, backend when running in Docker
REACT_APP_BACKEND_URL=http://localhost:5000

# Port for frontend dev server (default is 3000)
PORT=3000
